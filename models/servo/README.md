# Servo identification output

`python tools/simulation/analyze.py <step-recording.csv>` writes the complete
reviewable fit to `fit.json` in this directory. The smaller parameter set used
at runtime is versioned in `src/ballbal/simulation/params.json`.
